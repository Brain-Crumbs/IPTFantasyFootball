# bootstrap/

Bootstrap-only supporting documentation and placeholders live here.

This directory is **not** an implementation of the future control plane. BOOT-000 only establishes the boundary so later BOOT tasks have an explicit home for bootstrap-specific material.

Authority remains with the master issue and task-specific child issues until repository-native workflow state is implemented and cut over.
