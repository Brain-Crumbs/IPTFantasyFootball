{
  "schemaId": "ipt.module-contract",
  "schemaVersion": "1.1.0",
  "moduleId": "control-plane.cli-shell",
  "moduleVersion": "1.1.0",
  "structuralContract": {
    "interfaces": [
      "Human mode: npm run agent -- <command>",
      "Machine mode: npm run --silent agent -- --json <command>",
      "JSON envelope { schemaVersion, ok, command, data, error }",
      "Exit codes 0=SUCCESS, 2=USAGE_ERROR, 3=NOT_IMPLEMENTED, 70=INTERNAL_ERROR",
      "Implemented commands help, version, next"
    ]
  },
  "semanticContract": {
    "capabilities": [
      "deterministic-envelope",
      "deterministic-exit-codes",
      "reserved-command-failure",
      "reviewed-command-routing",
      "next-task-command",
      "provider-neutral-shell"
    ],
    "behavioralConstraints": [
      "successful commands exit 0",
      "expected usage errors exit 2",
      "reserved unimplemented commands exit 3",
      "unexpected internal failures exit 70",
      "JSON mode emits exactly one top-level envelope object for normal success or expected command failure",
      "documented npm machine invocation suppresses npm lifecycle logging so stdout remains exactly one CLI envelope",
      "next returns selected empty complete or blocked as command-specific data while preserving the top-level envelope",
      "invalid task or dependency repository input that prevents trustworthy selection fails rather than returning partial success"
    ],
    "invariants": [
      "identical argv and equivalent repository/task-state inputs produce deterministic normal command results",
      "shell behavior does not require an AI provider",
      "registration of a reserved command never implies implemented workflow semantics",
      "BOOT-008 next routing performs no assignment lifecycle mutation branch mutation review or merge behavior"
    ],
    "examples": [
      "npm run --silent agent -- --json version emits only a success envelope on stdout and exits 0",
      "npm run --silent agent -- --json next emits BOOT-008 selection data and exits 0 when evaluated successfully",
      "start exits 3 with COMMAND_NOT_IMPLEMENTED until BOOT-013"
    ],
    "edgeCases": [
      "empty argv resolves to help",
      "unknown option exits 2 with USAGE_UNKNOWN_OPTION",
      "unknown command exits 2 with USAGE_UNKNOWN_COMMAND",
      "--json may appear before or after the command token",
      "empty valid task registry makes next return a successful empty result"
    ]
  },
  "allowedDependencies": [
    "node:core",
    "contracts/*",
    "control-plane.task-registry",
    "control-plane.next-task"
  ],
  "forbiddenDependencies": [
    "agent-provider/*",
    "fantasy-product/*",
    "github-adapter/*",
    "lifecycle-mutation-engines/*",
    "assignment-lock-managers/*"
  ],
  "knownConsumers": [
    {
      "consumerId": "future-bootstrap-command-modules",
      "expectations": [
        "stable top-level envelope and documented exit-code meanings",
        "registered reserved commands do not report success without implemented behavior",
        "command-specific data can evolve additively without changing top-level envelope meanings"
      ],
      "requiredCapabilities": [
        "deterministic-envelope",
        "deterministic-exit-codes",
        "reserved-command-failure",
        "reviewed-command-routing"
      ],
      "acceptedRanges": [
        "exit codes {0, 2, 3, 70}",
        "documented command-specific success data"
      ],
      "requiredReachableRanges": [
        "exit 0 success",
        "exit 2 invalid usage",
        "exit 3 reserved unimplemented",
        "successful next data containing selected task ID and canonical branch"
      ]
    }
  ]
}
