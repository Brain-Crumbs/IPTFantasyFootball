# docs/

Human-readable architecture and operating documentation introduced by later bootstrap tasks belongs here.

BOOT-000 does not pre-author those later designs. Root governance currently consists of `AGENTS.md`, `README.md`, `CONSTITUTION.md`, and `BOOTSTRAP.md`, with `BOOTSTRAP_VERSION` providing the machine-simple bootstrap marker.
