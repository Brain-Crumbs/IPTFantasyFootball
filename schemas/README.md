# schemas/

Reserved for future versioned machine-readable schemas.

BOOT-003 owns the initial schema/versioning policy. The presence of this directory in BOOT-000 must not be interpreted as a schema contract already existing.
