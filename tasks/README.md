# tasks/

Reserved for future repository-native task definitions and adjacent human-readable task documentation.

BOOT-000 does not define the task schema, registry loader, dependency resolver, or lifecycle engine. Those are owned by later BOOT tasks, beginning with the schema and CLI phases in master issue #1.
